{%- set version = params.version | camel_case -%}
{{- template.set_file_name("pkg/rule/attributes/" ~ "type_" ~ version ~ ".go") -}}
// Code generated by otel-lint. DO NOT EDIT.
package attributes

import (
	"github.com/ymtdzzz/otel-lint/pkg/rule"
)

var RulesType{{ version }} = []*rule.AttributeRule{
{% for attr in ctx %}
{%- set attr_name = attr.name | pascal_case -%}
{#- Both `messaging.client.id` and `messaging.client_id` are converted to `MessagingClientId`... -#}
{% if attr.name == "messaging.client_id" %}
  {%- set attr_name = "MessagingClient_Id" -%}
{% endif %}
	ruleType{{ attr_name }}{{ version }},
{% endfor %}
}

{% for attr in ctx %}
{%- set attr_name = attr.name | pascal_case -%}
{% if attr.name == "messaging.client_id" %}
  {%- set attr_name = "MessagingClient_Id" -%}
{% endif %}
var ruleType{{ attr_name }}{{ version }} = &rule.AttributeRule{
	Name:         "type.{{attr.name}}",
	Title:        "The type of `{{ attr.name }}` should be {{ attr.type }}",
  {% if attr.type == "string" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeStr(sa, "{{ attr.name }}") },
  {% elif attr.type == "int" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeInt(sa, "{{ attr.name }}") },
  {% elif attr.type == "double" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeDouble(sa, "{{ attr.name }}") },
  {% elif attr.type == "boolean" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeBool(sa, "{{ attr.name }}") },
  {% elif attr.type == "string[]" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeStrSlice(sa, "{{ attr.name }}") },
  {% elif attr.type == "int[]" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeIntSlice(sa, "{{ attr.name }}") },
  {% elif attr.type == "double[]" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeDoubleSlice(sa, "{{ attr.name }}") },
  {% elif attr.type == "boolean[]" %}
	Check:        func(sa *rule.SignalAttributes) bool { return valueTypeBoolSlice(sa, "{{ attr.name }}") },
  {% endif %}
	Severity:     rule.SeverityError,
	Stability:    rule.{{ attr.stability | map_text("stabilities", "StabilityExperimental") }},
	Source:       "",
  Version:      "{{ params.version }}",
}

{% endfor %}
